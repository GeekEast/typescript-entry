export declare enum ProviderType {
    VALUE = "value",
    FACTORY = "factory",
    SERVICE = "service",
    PROVIDER = "provider",
    CONTROLLER = "controller",
    INTERCEPTOR = "interceptor",
    CONVERTER = "converter",
    FILTER = "filter",
    MIDDLEWARE = "middleware"
}
