import { OverrideProvider } from "./overrideProvider";
/**
 * Override a service which is already registered in ProviderRegistry.
 * @returns {Function}
 * @decorators
 * @param targetService
 */
export declare const OverrideService: typeof OverrideProvider;
