import { Type } from "@tsed/core";
import { IInterceptor } from "../interfaces";
/**
 * Attaches interceptor to method call and executes the before and after methods
 *
 * @param interceptor
 * @param options
 * @decorator
 */
export declare function Intercept<T extends IInterceptor>(interceptor: Type<T>, options?: any): Function;
