import { Type } from "@tsed/core";
import { Provider } from "@tsed/di";
import * as Express from "express";
import { IRouterSettings } from "../../config/interfaces/IServerSettings";
import { IControllerMiddlewares, IControllerProvider } from "../interfaces";
import { EndpointMetadata } from "./EndpointMetadata";
export interface IChildrenController extends Type<any> {
    $parentCtrl?: ControllerProvider;
}
export declare class ControllerProvider extends Provider<any> implements IControllerProvider {
    router: Express.Router & {
        [index: string]: any;
    };
    /**
     * The path for the controller
     */
    path: string;
    /**
     * Controllers that depend to this controller.
     * @type {Array}
     * @private
     */
    private _children;
    constructor(provide: any);
    /**
     *
     * @returns {Endpoint[]}
     */
    readonly endpoints: EndpointMetadata[];
    /**
     *
     * @returns {Type<any>[]}
     */
    /**
    *
    * @param children
    */
    children: IChildrenController[];
    /**
     *
     * @returns {IRouterSettings}
     */
    /**
    *
    * @param value
    */
    routerOptions: IRouterSettings;
    /**
     *
     * @returns {ControllerProvider}
     */
    readonly parent: any;
    /**
     *
     * @returns {any[]}
     */
    /**
    *
    * @param middlewares
    */
    middlewares: IControllerMiddlewares;
    /**
     * Resolve final endpoint url.
     */
    getEndpointUrl(routerPath?: string): string;
    /**
     *
     */
    hasEndpointUrl(): boolean;
    /**
     *
     * @returns {boolean}
     */
    hasChildren(): boolean;
    /**
     *
     * @returns {boolean}
     */
    hasParent(): boolean;
}
