import { InjectorService } from "@tsed/di";
import { EndpointMetadata } from "./EndpointMetadata";
import { HandlerMetadata } from "./HandlerMetadata";
/**
 * @stable
 */
export declare class HandlerBuilder {
    private handlerMetadata;
    private filters;
    private debug;
    private injector;
    constructor(handlerMetadata: HandlerMetadata);
    /**
     *
     * @param obj
     * @returns {HandlerBuilder}
     */
    static from(obj: any | EndpointMetadata): {
        build(injector: InjectorService): any;
    };
    static resolve(obj: any | EndpointMetadata, injector: InjectorService): HandlerMetadata;
    static handle(process: any, { request, response, next, hasNextFunction }: any): any;
    /**
     *
     * @returns {any}
     */
    build(injector: InjectorService): (err: any, request: any, response: any, next: any) => Promise<any>;
    /**
     *
     */
    private getHandler;
    /**
     *
     * @returns {Promise<any>}
     * @param request
     * @param response
     * @param next
     * @param err
     */
    private invoke;
    /**
     *
     * @param {Express.Request} request
     * @param o
     * @returns {string}
     */
    private log;
    /**
     *
     * @param {Express.Request} request
     * @param {Express.Response} response
     * @param {Express.NextFunction} next
     * @returns {any}
     */
    private buildNext;
    /**
     *
     * @param request
     * @param response
     * @param next
     * @param err
     */
    private runFilters;
}
