import { LocalsContainer } from "@tsed/di";
import { EndpointMetadata } from "./EndpointMetadata";
export declare class Context extends Map<any, any> {
    readonly id: string;
    readonly dateStart: Date;
    readonly container: LocalsContainer<any>;
    endpoint: EndpointMetadata;
    data: any;
    [key: string]: any;
    constructor({ id }: {
        id: string;
    });
    destroy(): Promise<void>;
}
