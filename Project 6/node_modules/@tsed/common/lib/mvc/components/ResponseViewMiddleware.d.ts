import { EndpointInfo, Res } from "../../filters";
import { IMiddleware } from "../interfaces";
/**
 * See example to override ResponseViewMiddleware [here](/docs/middlewares/override/response-view.md).
 * @middleware
 */
export declare class ResponseViewMiddleware implements IMiddleware {
    use(data: any, endpoint: EndpointInfo, response: Res): Promise<{}>;
}
