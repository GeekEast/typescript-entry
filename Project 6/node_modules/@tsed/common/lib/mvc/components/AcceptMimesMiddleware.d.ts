import { EndpointInfo, Req } from "../../filters";
import { IMiddleware } from "../interfaces";
/**
 * @middleware
 */
export declare class AcceptMimesMiddleware implements IMiddleware {
    use(endpoint: EndpointInfo, request: Req): void;
}
