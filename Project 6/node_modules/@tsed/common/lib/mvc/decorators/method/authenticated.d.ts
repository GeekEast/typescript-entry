import { IAuthOptions } from "./authOptions";
/**
 * Use passport authentication strategy on your endpoint.
 *
 * ```typescript
 * @Controller('/mypath')
 * class MyCtrl {
 *
 *   @Get('/')
 *   @Authenticated({role: 'admin'})
 *   public getResource(){}
 * }
 * ```
 *
 * @param options
 * @returns {Function}
 * @decorator
 * @endpoint
 */
export declare function Authenticated(options?: IAuthOptions): Function;
