import { InjectorService } from "@tsed/di";
import * as Express from "express";
/**
 * Bind request and create a new context to store information during the request lifecycle
 * @param injector
 */
export declare function contextMiddleware(injector: InjectorService): (request: Express.Request, response: Express.Response, next: Express.NextFunction) => Promise<void>;
