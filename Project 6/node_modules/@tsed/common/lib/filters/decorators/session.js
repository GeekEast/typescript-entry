"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const ParamTypes_1 = require("../interfaces/ParamTypes");
const SessionFilter_1 = require("../components/SessionFilter");
const ParamRegistry_1 = require("../registries/ParamRegistry");
/**
 * Session return the value from [request.session](http://expressjs.com/en/4x/api.html#req.session) object.
 *
 * #### Example
 *
 * ```typescript
 * @Controller('/')
 * class MyCtrl {
 *    @Post('/')
 *    create(@Session() session: Express.Session) {
 *       console.log('Entire session', session);
 *    }
 *
 *    @Post('/')
 *    create(@Session('id') id: string) {
 *       console.log('ID', id);
 *    }
 * }
 * ```
 * > For more information on deserialization see [converters](/docs/converters.md) page.
 *
 * @param expression The path of the property to get.
 * @param useType The type of the class that to be used to deserialize the data.
 * @decorator
 * @returns {Function}
 */
function Session(expression, useType) {
    return ParamRegistry_1.ParamRegistry.decorate(SessionFilter_1.SessionFilter, {
        expression,
        useType,
        paramType: ParamTypes_1.ParamTypes.SESSION
    });
}
exports.Session = Session;

//# sourceMappingURL=session.js.map
