"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 *
 * @param {Promise<any>} promise
 * @param {number} time
 * @returns {Promise<any>}
 */
function promiseTimeout(promise, time = 1000) {
    const timeout = (promise, time) => new Promise(resolve => {
        promise.then(response => {
            resolve();
            return response;
        });
        setTimeout(() => resolve({ ok: false }), time);
    });
    promise = promise.then(response => ({ ok: true, response }));
    return Promise.race([promise, timeout(promise, time)]);
}
exports.promiseTimeout = promiseTimeout;

//# sourceMappingURL=promiseTimeout.js.map
