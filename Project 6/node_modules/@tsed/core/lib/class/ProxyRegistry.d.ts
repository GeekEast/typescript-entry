import { ProxyMap } from "./ProxyMap";
import { Registry, RegistryKey } from "./Registry";
/**
 * @private
 */
export declare abstract class ProxyRegistry<T, I> extends ProxyMap<RegistryKey, T> {
    protected registry: Registry<T, I>;
    constructor(registry: Registry<T, I>);
    set(key: RegistryKey, value: T): this;
}
